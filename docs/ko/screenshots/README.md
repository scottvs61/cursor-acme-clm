# Screenshots for KO documents

Add PNG or JPG files here so the KO docs can display them. Suggested captures:

| File | Description | Used in |
|------|-------------|---------|
| `clm-certificates-list.png` | CLM UI: sidebar + Certificates tab, certificate table (at least one row). | KO-CLM-API.md |
| `clm-certificate-detail.png` | CLM UI: Certificate details panel (after clicking "Details" on a row). | KO-CLM-API.md |
| `clm-bulk-enroll.png` | CLM UI: Bulk Enroll tab, form with Default Product ID and CSRs textarea. | KO-CLM-API.md |
| `clm-bulk-enroll-result.png` | CLM UI: Bulk enroll result table (succeeded/failed per row). | KO-CLM-API.md |
| `clm-scep.png` | CLM UI: SCEP tab, endpoints table and usage notes. | KO-CLM-API.md |

**How to capture:** Run the CLM UI (e.g. `http://localhost:8001`), open each page, and save a full-window or relevant region as the filename above. Then the KO will render the image automatically.
